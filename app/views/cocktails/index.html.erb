<% content_for(:banner) do %>
<%= render 'shared/banner' %>
<% end %>

<div id="content-under" class="container">
  <div class="row">
    <% @cocktails.each do |c| %>
    <div class="col-xs-12 col-sm-4">
      <div class="card">
       <div class="card-body" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path c.photo unless c.photo.blank?%>');">
        <div class="card-description">
            <h2><%= link_to c.name, cocktail_path(c) %></h2>
          </div>
          <%= link_to "", cocktail_path(c), class: "card-link"  %>
        <div class="card-footer">
          <div class="card-date">
            <% doses = Dose.where(cocktail_id: c.id)  %>
            <p><%= doses.count %> Ingredients </p>
          </div>
        </div>
       </div>
      </div>
    </div>
    <% end %>
  </div>
</div>
